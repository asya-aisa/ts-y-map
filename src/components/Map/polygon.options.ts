interface IPolygonOptions {
	fillColor: string
	strokeColor: string
	opacity: number
	strokeWidth: number
	strokeStyle: string
}

export const polygonOptions:IPolygonOptions = {
	fillColor: '#00FF00',
	strokeColor: '#0000FF',
	opacity: 0.5,
	strokeWidth: 3,
	strokeStyle: 'solid',
}
